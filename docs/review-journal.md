# Review Journal

This journal records the domain cases that matter before widening the public API.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its algorithms focus without claiming live deployment or external usage.

## Cases

- `baseline`: `input width`, score 91, lane `hold`
- `stress`: `search depth`, score 108, lane `watch`
- `edge`: `boundary pressure`, score 186, lane `ship`
- `recovery`: `complexity`, score 116, lane `watch`
- `stale`: `input width`, score 184, lane `ship`

## Note

The repository should be understandable without pretending it is larger than it is.
